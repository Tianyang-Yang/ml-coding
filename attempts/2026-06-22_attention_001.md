# 2026-06-22 — Attention 001: Scaled Dot-Product Attention (First Attempt)

**Question:** `questions/attention/001_scaled_dot_product_attention.md`  
**Date:** 2026-06-22  
**Time taken:** ~20 min

---

## My Implementation

```python
import math, torch, torch.nn.functional as F

def scaled_dot_product_attention(Q, K, V, mask=None):
    d_k = Q.size(-1)
    scores = torch.matmul(Q, K.transpose(-2, -1)) / math.sqrt(d_k)
    if mask is not None:
        scores = scores.masked_fill(mask, float("-inf"))
    weights = F.softmax(scores, dim=-1)
    return torch.matmul(weights, V)
```

## Self-Assessment

| Criterion | Pass? | Notes |
|---|---|---|
| Correct formula | ✅ | QKᵀ / √dₖ |
| Mask handling | ✅ | Used `masked_fill` |
| Output shape | ✅ | (batch, heads, seq_q, d_v) |
| No built-in helpers | ✅ | |

## Follow-up Answers

1. **Why √dₖ?** Dot products grow in magnitude with dₖ (variance ≈ dₖ for
   random Q, K). Dividing by √dₖ brings variance back to ~1 and prevents
   softmax saturation.

2. **Complexity?** O(n² · dₖ) — quadratic in sequence length.

3. **Additive vs multiplicative?** Additive (Bahdanau) uses a feed-forward
   network to combine Q and K; multiplicative (Luong / Vaswani) uses dot
   product — faster due to optimised matrix ops.

## Mistakes Made

- Initially forgot the `dim=-1` argument to `softmax`.
- Was uncertain about transpose — needed `-2, -1` not just `.T`.

## Next Steps

- Practice implementing `MultiHeadAttention` (question 002).
- Review RoPE encoding for positional information.
